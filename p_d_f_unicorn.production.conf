{
    hypnotoad => {
        #listen  => ['https://*:443?cert=/etc/server.crt&key=/etc/server.key'],
        listen  => ['http://*:3001'],
        workers => 10
    },
    stripe => {
        secret_api_key => '',
        public_api_key => '',
    },
    media_directory => '/pdf_unicorn/images',
    mongodb => {
        connect => 'mongodb://<user>:<pass>@<server>/pdfunicorn'
    }
};